<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>UNO Lobby</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="lobby-page">

    <main class="container py-4 py-md-5">
        <div class="lobby-container card border-0 shadow-lg mx-auto">
            <div class="card-body p-4 p-md-5">
                <p class="text-uppercase fw-semibold text-body-secondary small mb-2">Room Lobby</p>
                <h1 class="h2 mb-4">Room: <span class="room-code"><%= roomId %></span></h1>

                <div class="player-list mb-4">
                    <% players.forEach(player => { %>
                        <div class="player-item">
                            <span class="badge rounded-pill bg-danger-subtle text-danger-emphasis px-3 py-2">Player</span>
                            <span><%= player %></span>
                        </div>
                    <% }) %>
                </div>

                <div class="lobby-actions d-flex flex-column flex-sm-row gap-3 justify-content-center">
                    <button class="start-btn hidden btn btn-success btn-lg px-4">Start Game</button>
                    <button class="leave-btn btn btn-outline-danger btn-lg px-4">Leave</button>
                </div>
            </div>
        </div>
    </main>
<script src="/socket.io/socket.io.js"></script>
<script>
  const roomId = "<%= roomId %>";
</script>
<script src="/js/lobby.js"></script>
</body>
</html>
